# Dev Lane — Captured Inventory Snapshot

> [!NOTE]
> This file is an operational state snapshot automatically generated by [`capture-lane.sh`](./capture-lane.sh). It records the active LiveKit trunk IDs and routing configuration of a deployed instance for drift verification and disaster recovery diffing. Do not edit this file manually.
>
> The `worker.env — keys present` list below is regenerated by `capture-lane.sh` from the box's live `worker.env`, so it shows what THAT capture found, not what the template requires. This capture predates the pepper rollout: a lane rendered from today's `lane.env` also carries `RT_PHONE_HASH_PEPPER` and `RT_REQUIRE_PEPPER` (see `deploy/worker.env.example`), and the list will show them on the next capture.

To refresh or generate an inventory snapshot for any target lane:
```bash
HOST=root@<box-ip> LANE=<lane-name> bash deploy/provisioning/capture-lane.sh
```

## Identity

| What | Value |
|---|---|
| Number | `+19736069515` |
| Agent name | `phone-pal-dev` — must equal the dispatch rule's agent |
| Box IP | `173.255.235.130` — Twilio routes here BY IP; keep the reservation |
| LiveKit API key | fingerprint `8eca198c82dd` (never stored) |
| LiveKit secret | fingerprint `841c8257720b` (never stored) |
| Outbound trunk | `ST_r3dJm55sk6aQ` |

## LiveKit SIP state

### Inbound trunks
```
┌─────────────────┬───────────────────┬──────────────┬────────────────────────────────────────────────┬────────────────┬────────────────┬────────────┬─────────┬──────────┐
│ SipTrunkID      │ Name              │ Numbers      │ AllowedAddresses                               │ AllowedNumbers │ Authentication │ Encryption │ Headers │ Metadata │
├─────────────────┼───────────────────┼──────────────┼────────────────────────────────────────────────┼────────────────┼────────────────┼────────────┼─────────┼──────────┤
│ ST_GH2EDgwJtwZJ │ phone-pal-dev-606 │ +19736069515 │ 192.76.120.0/24,64.16.250.0/24,185.246.41.0/24 │                │                │ DISABLE    │         │          │
└─────────────────┴───────────────────┴──────────────┴────────────────────────────────────────────────┴────────────────┴────────────────┴────────────┴─────────┴──────────┘
```
### Dispatch rules
```
┌───────────────────┬───────────────────┬─────────────────┬─────────────────────┬──────────────────────────┬─────┬────────────┬───────────────┐
│ SipDispatchRuleID │ Name              │ SipTrunks       │ Type                │ RoomName                 │ Pin │ Attributes │ Agents        │
├───────────────────┼───────────────────┼─────────────────┼─────────────────────┼──────────────────────────┼─────┼────────────┼───────────────┤
│ SDR_fKTdjHsssPBu  │ phone-pal-dev-606 │ ST_GH2EDgwJtwZJ │ Individual (Caller) │ phone-_<caller>_<random> │     │ map[]      │ phone-pal-dev │
└───────────────────┴───────────────────┴─────────────────┴─────────────────────┴──────────────────────────┴─────┴────────────┴───────────────┘
```
### Outbound trunks
```
┌─────────────────┬────────────────────────┬────────────────┬───────────┬──────────────┬───────────────────────┬────────────┬─────────┬──────────┐
│ SipTrunkID      │ Name                   │ Address        │ Transport │ Numbers      │ Authentication        │ Encryption │ Headers │ Metadata │
├─────────────────┼────────────────────────┼────────────────┼───────────┼──────────────┼───────────────────────┼────────────┼─────────┼──────────┤
│ ST_r3dJm55sk6aQ │ phone-pal-dev-outbound │ sip.telnyx.com │ AUTO      │ +19736069515 │ phonepaldevout / **** │ DISABLE    │         │          │
(pre-2026-09-02 capture: the address is now <TWILIO_TRUNK_DOMAIN>.pstn.twilio.com — re-capture this lane)
└─────────────────┴────────────────────────┴────────────────┴───────────┴──────────────┴───────────────────────┴────────────┴─────────┴──────────┘
```
## Containers
```
phone-pal-caddy-1	caddy:2-alpine	Up 2 weeks
phone-pal-livekit-1	livekit/livekit-server:latest	Up 2 weeks
phone-pal-redis-1	redis:7-alpine	Up 2 weeks
phone-pal-scheduler-1	phone-pal-scheduler	Up 2 minutes
phone-pal-sip-1	livekit/sip:latest	Up 2 weeks
phone-pal-worker-1	phone-pal-worker	Up 2 minutes
```
## worker.env — keys present (values deliberately absent)

A rebuilt lane needs every one of these set. The values come from
`lane.env`, which is not in this repository.

```
AGENT_NAME
DEFAULT_TZ
ENV_MODE
GOOGLE_API_KEY
LIVEKIT_API_KEY
LIVEKIT_API_SECRET
LIVEKIT_SELF_HOSTED
LIVEKIT_URL
LK_GOOGLE_DEBUG
RESEND_API_KEY
RT_ALERT_EMAIL_TO
RT_ALLOWED_SUPABASE_REF
RT_CALLBACK_HOURS
RT_CALLBACK_HOURS_LIVE
RT_DAILY_MINUTES_PER_CALLER
RT_GREET
RT_HEALTH_PORT
RT_LANGUAGE
RT_LOG_LEVEL
RT_PUBLIC_NUMBER
RT_RECOVERY
RT_SCHEDULER_ENABLED
RT_TRACE
SIP_OUTBOUND_TRUNK_ID
SUPABASE_PROJECT_REF
SUPABASE_SERVICE_ROLE_KEY
SUPABASE_URL
```
